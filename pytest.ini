[pytest]
# `pytest` from the repo root runs the backend/*.py suite (backend.main).
# backend/app has its own suite: cd backend && pytest tests/
testpaths = backend
python_files = test_*.py
norecursedirs = tests app .* __pycache__
# lets backend/tests keep importing `app.*` even though backend/ is a package
pythonpath = backend
